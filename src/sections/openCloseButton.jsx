import OpenCloseButton from "../components/OpenCloseButton";

const OpenCloseButtonContainer = () => {
  return (
    <div className="w-full min-h-screen flex flex-wrap gap-10 justify-center items-center bg-violet-700">
      <OpenCloseButton />
    </div>
  );
};

export default OpenCloseButtonContainer;
